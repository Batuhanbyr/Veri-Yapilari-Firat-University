package Stacks;
import java.util.*; // import everything from util 
public class Main {

	public static void main(String[] args) {
		// stack = LIFO data structure. Last In Fırst Out
		// stocks objects into a sort of "vertical tower"
		// push() to add to the top 
		// pop() for remove from the top
		
		//Stack<String> stack = new Stack<String>(); 
		
		//System.out.println(stack.empty()); // this method checks if stacks empty
		
		
       /* stack.push("Minecraft"); // first element
        stack.push("Skyrim");
        stack.push("DOOM");
        stack.push("Borderlands");
        stack.push("FFVII");*/
        
      /*  stack.pop(); // it will remove last element
        stack.pop();
        stack.pop();
        stack.pop();
        stack.pop();*/
        //stack.pop(); // ! empty stack exception !
        
       // String MyGame = stack.pop();
       // System.out.println(MyGame);
        
       // System.out.println(stack.peek()); // it's keeps the last element but don't remove like pop
        
        //System.out.println(stack.empty()); // this method checks if stacks empty ( isEmpty() ) 
        //System.out.println(stack);
       // System.out.println(stack.search("DOOM")); //it^s return index of value (starts with 1) if it's not in the stack returns -1
       
		
		//---EXAMPLES---
        
		//-------------------------------------------------------------------------------------
		
		/*
		// Creating an empty Stack
        Stack<String> stack = new Stack<String>();
 
        // Use push() to add elements into the Stack
        stack.push("Welcome");
        stack.push("To");
        stack.push("Geeks");
        stack.push("For");
        stack.push("Geeks");
 
        // Displaying the Stack
        System.out.println("Initial Stack: " + stack);
 
        // Fetching the element at the head of the Stack
        System.out.println("The element at the top of the"
                           + " stack is: " + stack.peek());
 
        // Displaying the Stack after the Operation
        System.out.println("Final Stack: " + stack);  */
		
		//-------------------------------------------------------------------------------------
		
		
		// Creating an empty Stack
        /*Stack<Integer> stack = new Stack<Integer>();
 
        // Use add() method to add elements
        stack.push(10);
        stack.push(15);
        stack.push(30);
        stack.push(20);
        stack.push(5);
 
        // Displaying the Stack
        System.out.println("Initial Stack: " + stack);
 
        // Removing elements using pop() method
        System.out.println("Popped element: "
                           + stack.pop());
        System.out.println("Popped element: "
                           + stack.pop());
 
        // Displaying the Stack after pop operation
        System.out.println("Stack after pop operation "
                           + stack); */
		
		//-------------------------------------------------------------------------------------
        
		// Create a new stack
        /*Stack<Integer> stack = new Stack<>();
 
        // Push elements onto the stack
        stack.push(1);
        stack.push(2);
        stack.push(3);
        stack.push(4);
 
        // Pop elements from the stack
        while(!stack.isEmpty()) {
            System.out.println(stack.pop());
        }*/
		
		
		//-------------------------------------------------------------------------------------
		
		
	}

}
